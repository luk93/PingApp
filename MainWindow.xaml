<Window x:Class="PingApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tools="clr-namespace:PingApp.Tools"
        xmlns:local="clr-namespace:PingApp"
        xmlns:controls="clr-namespace:PingApp.Controls"
        mc:Ignorable="d"
        Title="Ping App" Height="600" Width="910" MinHeight="600" MinWidth="910"
        Background="#546A76"
        Foreground="#F5E9E2" Icon="/icons8-ping-96.png"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <controls:PingAppRibbon Grid.Row="0" DataContext="{Binding Ribbon}"/>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <ContentControl Content="{Binding Navigator.CurrentViewModel}"/>
        </ScrollViewer>
        <controls:PingAppStatusBar Grid.Row="2" DataContext="{Binding StatusBar}"/>
    </Grid>
</Window>
